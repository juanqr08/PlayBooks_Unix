---
- name: Create users with home directory
  win_user: 
    name: "{{ item.username }}"
    fullname: "{{ item.username }}"
    description: "Usuario Creado Bajo el Caso"
    password: "{{ item.password }}"
    state: present
  with_items: 
    - "{{ users }}"
